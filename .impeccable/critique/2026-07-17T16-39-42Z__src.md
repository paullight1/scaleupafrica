---
target: whole app (src)
total_score: 21
p0_count: 2
p1_count: 4
timestamp: 2026-07-17T16-39-42Z
slug: src
---
Method: dual-agent (A: design review sub-agent · B: detector sub-agent). Source inspection only — no browser automation available; contrast ratios computed from HSL tokens.

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 2 | Text-only loading; ~60s AI call shows only a button spinner (Funding.tsx:207), no skeleton or expectation-setting |
| 2 | Match System / Real World | 3 | Local pricing & sectors good; "Funding Radar", "keywords" jargon for non-technical founders |
| 3 | User Control and Freedom | 1 | signOut never called anywhere (useAuth.tsx:40); no profile delete UI; paywall exit loops |
| 4 | Consistency and Standards | 2 | Native select vs shadcn Select; dual toasters; nav exists only on landing page |
| 5 | Error Prevention | 2 | Publishing personal phone/WhatsApp publicly has one passive warning line (CreateProfile.tsx:122) |
| 6 | Recognition Rather Than Recall | 2 | Validation toast says just "Required" among 15 fields; paywall→pricing hash never scrolls |
| 7 | Flexibility and Efficiency | 2 | No directory filters despite structured data; keywords column never used; no search pre-fill |
| 8 | Aesthetic and Minimalist Design | 3 | Restrained sections, good progressive disclosure on funding cards; docked for template uniformity |
| 9 | Error Recovery | 1 | Errors swallowed into lying states: failures render "No profiles yet" (Directory.tsx:33) or the paywall to paying members (Funding.tsx:90) |
| 10 | Help and Documentation | 3 | Substantive FAQ, excellent point-of-need fraud warnings; none on form pages |
| **Total** | | **21/40** | **Acceptable — significant improvements needed** |

## Anti-Patterns Verdict

LLM assessment: yes, reads AI-generated (mid-tier). Tells: index.html still fully Lovable-branded ("Lovable App" title/OG); gradient gold text on every h1 including product pages; identical uppercase tracked eyebrows on 4 sections; Sparkles hero pill; identical icon-card grid; uniform whileInView fade on ~14 blocks; Playfair+Inter default pairing; template copy formulas. Saved from bottom-tier by coherent forest/gold token system and genuinely human anti-fraud/disclaimer copy.

Deterministic scan: 1 finding — overused-font (Inter) at src/index.css:131. Detector missed Tailwind-class-level patterns (gradient text, eyebrows) that the LLM caught; no false positives. Browser overlays skipped (no automation, deps not installed).

## Priority Issues

1. [P0] Paid funnel is a closed loop — no payment mechanism exists; paywall CTA → /#pricing never scrolls; pricing CTA → profile form; copy promises "Immediate access" and a payment processor that don't exist (Funding.tsx:162, Pricing.tsx:195,220, FAQ.tsx:33). Fix: honest concierge flow + working CTA path. → /impeccable shape + harden
2. [P0] No sign-out, no authenticated nav anywhere; header only on landing, always logged-out state (useAuth.tsx:40, Header.tsx). → /impeccable shape
3. [P1] Swallowed fetch errors produce lying UI (empty state / paywall on network failure) (Directory.tsx:33-38, Funding.tsx:90-93, CreateProfile.tsx:64). → /impeccable harden
4. [P1] AI results ephemeral + uncached; every run re-bills Gemini 2.5 Pro 16k tokens; no rate limit (Funding.tsx:75, edge fn). → /impeccable harden
5. [P1] WCAG contrast failures (gold-on-light ≈2.0:1, eyebrows ≈3.1:1, footer /40 ≈2.4:1) + zero prefers-reduced-motion handling. → /impeccable audit + colorize
6. [P1] Mobile drawer "Sign in" is forest-on-forest, invisible (Header.tsx:67,81). → /impeccable polish
7. [P2] Half the profile schema write-only: long_description, whatsapp, twitter, founder_photo collected but rendered nowhere; no /directory/:id detail page. → /impeccable craft
8. [P2] Performance vs 3G audience: no code splitting, raw <a> full reloads in header, 253KB decorative hero, 10 render-blocking font weights. → /impeccable optimize

## Persona Red Flags

Casey (mobile 3G): full reloads from landing nav <a> tags; blank green void before JS animation reveals content; minute-long AI call w/ button spinner only; sub-44px tap targets on card contact links; invisible drawer sign-in.
Jordan (first-timer): "keywords" mental model with no pre-seed from profile; "Required" toast w/o field name; publish dumps to directory top with no "here's your profile" payoff; Instagram field ambiguity.
Sam (screen reader/keyboard): unlabeled menu toggle (no aria-expanded); placeholder-only inputs; expanders without aria-expanded; no aria-live for async states; no focus management on route change; universal opacity animation w/o reduced-motion.
Amara (Lagos founder, scam-skeptical): ₦95,000/yr with no payment method, no company identity, dead Terms/Privacy links = scam fingerprint; FAQ claims nonexistent mobile-money processor; network blip shows paying member the paywall; personal phone/WhatsApp published with one line of notice.

## Minor Observations

document.title unset on Auth/landing (stays "Lovable App"); dual toast systems; hardcoded FX table w/ fake "EU" country code; hero CTA #2 is 3 hops to a paywall; NotFound unbranded with console.error; dead has_active_subscription() SQL fn; decorative hero image has non-empty alt; <Link><Button/></Link> nesting instead of asChild; migration redefines tg_set_updated_at twice.

## Questions to Consider

1. What if manual/concierge subscription (WhatsApp + mobile money + human activation) were the brand instead of the bug?
2. Why per-user on-demand LLM generation instead of a weekly cached, spot-checked, auditable feed?
3. Where's the shareable /directory/:slug page — the actual growth loop?
4. What would the landing look like if the Disclaimer's honest voice were the identity instead of gold gradients?
5. Who is the home page for — the 1st visit or the 100th?
