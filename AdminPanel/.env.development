# Dev-only (Vite loads .env.development for `vite dev`, never for a build).
#
# The admin panel owns its sign-in screen. This URL is only for deliberate links
# back to the public site when the apps use separate origins in development.
VITE_SITE_URL=http://localhost:8080
