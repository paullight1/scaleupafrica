{
  "name": "cresciva",
  "private": true,
  "version": "0.1.0",
  "description": "Pan-African SME directory and funding intelligence.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "Shared",
    "Frontend",
    "AdminPanel",
    "Backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace Frontend",
    "dev:admin": "npm run dev --workspace AdminPanel",
    "dev:api": "npm run start:dev --workspace Backend",
    "build": "npm run sitemap && npm run build --workspace Frontend && npm run build --workspace AdminPanel && npm run assemble",
    "build:web": "npm run sitemap && npm run build --workspace Frontend",
    "sitemap": "node scripts/generate-sitemap.mjs",
    "og": "node scripts/generate-og-screenshot.mjs",
    "assemble": "node scripts/assemble-dist.mjs",
    "build:admin": "npm run build --workspace AdminPanel",
    "build:api": "npm run build --workspace Backend",
    "lint": "npm run lint --workspace Frontend && npm run lint --workspace AdminPanel && npm run lint --workspace Backend",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "verify": "npm run lint && npm run typecheck && npm run test && npm run build && npm run build:api",
    "assets": "bash scripts/generate-assets.sh"
  },
  "overrides": {
    "nanoid": "3.3.18"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
